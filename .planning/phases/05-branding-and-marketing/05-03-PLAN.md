---
phase: 05-branding-and-marketing
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/(app)/layout.tsx
  - src/app/(auth)/layout.tsx
  - src/app/invite/accept/page.tsx
  - src/app/invite/[token]/page.tsx
  - src/actions/auth.ts
  - src/actions/feedback.ts
  - src/components/upload/UploadPageClient.tsx
  - src/inngest/client.ts
  - src/lib/email.ts
autonomous: true
requirements: [BRAND-01, BRAND-04]

must_haves:
  truths:
    - "App shell nav shows 'Diamond Mechanics' wordmark, not 'Softball Mechanics'"
    - "Auth pages (login, signup) show 'Diamond Mechanics' name above the form card"
    - "Auth page tagline is updated (not 'Coach smarter, throw harder')"
    - "Invite acceptance pages (/invite/accept and /invite/[token]) show the Diamond Mechanics brand name"
    - "Branded invite email copy says 'Diamond Mechanics' not 'Softball Mechanics'"
    - "Feedback email footer says 'Diamond Mechanics' not 'Softball Mechanics'"
    - "Consent text on upload page mentions 'baseball and softball mechanics' not just 'softball'"
    - "grep for 'Softball Mechanics' in src/ (case-sensitive user-facing strings) returns 0 results"
  artifacts:
    - path: "src/app/(app)/layout.tsx"
      provides: "App shell nav with Diamond Mechanics wordmark"
      contains: "Diamond Mechanics"
    - path: "src/app/(auth)/layout.tsx"
      provides: "Auth layout with Diamond Mechanics logo/wordmark above form"
      contains: "Diamond Mechanics"
    - path: "src/app/invite/accept/page.tsx"
      provides: "Invite accept page with Diamond Mechanics brand header"
      contains: "Diamond Mechanics"
    - path: "src/app/invite/[token]/page.tsx"
      provides: "Invite token page with Diamond Mechanics brand header"
      contains: "Diamond Mechanics"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "authenticated app nav"
      via: "Diamond Mechanics wordmark span"
      pattern: "Diamond Mechanics"
    - from: "src/actions/auth.ts"
      to: "athlete invite email body"
      via: "sendEmail() call"
      pattern: "Diamond Mechanics"
---

<objective>
Rebrand the internal app shell, auth pages, invite acceptance pages, email templates, and consent text from "Softball Mechanics" to "Diamond Mechanics". This plan covers every user-facing string in the authenticated app and transactional emails. No new features — pure rename and brand upgrade.

Purpose: Every touchpoint in the product consistently presents the Diamond Mechanics brand. Athletes and coaches never see the old name.
Output: Updated nav, auth layout, invite pages, email copy, consent text, and Inngest client id.
</objective>

<execution_context>
@/Users/abhishekhodavdekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abhishekhodavdekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-branding-and-marketing/05-01-SUMMARY.md

<interfaces>
<!-- Current "Softball Mechanics" strings found via grep — all must be updated: -->

src/app/(app)/layout.tsx line 12:
  <span className="font-bold text-gray-900">Softball Mechanics</span>

src/app/(auth)/layout.tsx line 6-7:
  <h1 className="text-2xl font-bold text-gray-900">Softball Mechanics</h1>
  <p className="text-gray-500 text-sm mt-1">Coach smarter, throw harder</p>

src/actions/auth.ts line 128 (email HTML body):
  Your coach has invited you to the Softball Mechanics app...

src/actions/feedback.ts line 58 (email footer):
  You received this because your coach sent you mechanics feedback via the Softball Mechanics app.

src/components/upload/UploadPageClient.tsx line 101 (consent text):
  ...used solely for softball mechanics analysis.

src/inngest/client.ts line 3:
  export const inngest = new Inngest({ id: 'softball-mechanics' })

<!-- NOTE on inngest id: The Inngest app id 'softball-mechanics' is an internal identifier used
     in the Inngest dashboard. Changing it will cause Inngest Cloud to treat the app as new —
     acceptable for a rename, but the executor should update it to 'diamond-mechanics' to
     complete the full rebrand. -->

<!-- NOTE on invite pages: src/app/invite/accept/page.tsx and src/app/invite/[token]/page.tsx
     currently have no branding in their loading/success/error states. Add a branded header
     with Diamond Mechanics wordmark above the status content in each state. Both pages are
     'use client' components with min-h-screen flex wrappers — add the logo above each
     status div, not replacing the status content. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand app shell nav and auth layout</name>
  <files>src/app/(app)/layout.tsx, src/app/(auth)/layout.tsx</files>
  <action>
**src/app/(app)/layout.tsx:**
Replace `<span className="font-bold text-gray-900">Softball Mechanics</span>` with the Diamond Mechanics wordmark. Add a diamond icon using an inline SVG or a simple ◆ unicode character for the logo mark, plus the brand name in bold:

```tsx
<div className="flex items-center gap-2">
  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
    <path d="M10 2L18 10L10 18L2 10L10 2Z" fill="currentColor" className="text-primary" />
  </svg>
  <span className="font-extrabold text-gray-900">Diamond Mechanics</span>
</div>
```

If the SVG introduces complexity, fall back to a simpler wordmark-only: `<span className="font-extrabold text-gray-900">Diamond Mechanics</span>`. The logo treatment is Claude's discretion — a clean wordmark with the diamond icon is preferred.

**src/app/(auth)/layout.tsx:**
1. Replace `<h1 className="text-2xl font-bold text-gray-900">Softball Mechanics</h1>` with:
   ```tsx
   <div className="flex items-center justify-center gap-2 mb-1">
     <svg width="24" height="24" viewBox="0 0 20 20" fill="none" aria-hidden="true">
       <path d="M10 2L18 10L10 18L2 10L10 2Z" fill="currentColor" className="text-primary" />
     </svg>
     <span className="text-2xl font-extrabold text-gray-900">Diamond Mechanics</span>
   </div>
   ```
2. Replace the tagline: `<p className="text-gray-500 text-sm mt-1">Coach smarter, throw harder</p>` → `<p className="text-gray-500 text-sm mt-1">Coach smarter. Develop every athlete.</p>`

Use `className="text-primary"` on the SVG path, not a hardcoded fill color — this uses the brand blue token from Plan 01.
  </action>
  <verify>
    <automated>cd /Users/abhishekhodavdekar/git/softball-mechanics && grep -n "Diamond Mechanics" src/app/(app)/layout.tsx src/app/(auth)/layout.tsx</automated>
  </verify>
  <done>
  - `grep "Softball Mechanics" src/app/(app)/layout.tsx` returns 0 results
  - `grep "Softball Mechanics" src/app/(auth)/layout.tsx` returns 0 results
  - "Diamond Mechanics" appears in both files
  - Auth layout tagline updated (no "throw harder")
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebrand invite pages, email copy, consent text, and Inngest client</name>
  <files>src/app/invite/accept/page.tsx, src/app/invite/[token]/page.tsx, src/actions/auth.ts, src/actions/feedback.ts, src/components/upload/UploadPageClient.tsx, src/inngest/client.ts, src/lib/email.ts</files>
  <action>
**src/app/invite/accept/page.tsx** — Add a Diamond Mechanics branded header above each status state. The page has three return branches (loading, success, error). In each branch, above the status content div, add:
```tsx
<div className="mb-8 text-center">
  <span className="text-xl font-extrabold text-gray-900">Diamond Mechanics</span>
</div>
```
The outer wrapper `div className="min-h-screen flex items-center justify-center"` must remain. Add the brand header inside the text-center div for loading/success, or inside the max-w-md div for error. Adjust as needed so it reads naturally above the status message.

**src/app/invite/[token]/page.tsx** — Same treatment: add a Diamond Mechanics brand wordmark above each of the three status states (loading, success, error), following the same pattern as accept/page.tsx above.

**src/actions/auth.ts** — In the branded invite email HTML (around line 128), replace:
- `"Your coach has invited you to the Softball Mechanics app"` → `"Your coach has invited you to Diamond Mechanics"`
- Update the email subject if it contains "Softball Mechanics" (check line ~125 — the subject is already generic, no change needed if so)
- Update any other "Softball Mechanics" occurrences in this file

**src/actions/feedback.ts** — In the feedback email footer (around line 58), replace:
- `"via the Softball Mechanics app"` → `"via Diamond Mechanics"`

**src/components/upload/UploadPageClient.tsx** — In the consent checkbox text (around line 101), replace:
- `"used solely for softball mechanics analysis"` → `"used solely for baseball and softball mechanics analysis"`

**src/inngest/client.ts** — Update the Inngest app id:
- `{ id: 'softball-mechanics' }` → `{ id: 'diamond-mechanics' }`

**src/lib/email.ts** — Grep this file first:
```bash
grep -n "Softball Mechanics\|softball mechanics" src/lib/email.ts
```
Replace any "Softball Mechanics" occurrences found (e.g. email footer text, sender name, or subject defaults) with "Diamond Mechanics". If no matches are found, no change is needed — document that result in the SUMMARY.

After making all changes, run the grep sanity check:
```bash
grep -rn "Softball Mechanics\|softball mechanics" src/ --include="*.ts" --include="*.tsx"
```
Remaining instances should only be in: code comments, source citations in flags.ts/angles.ts (these are scientific paper references, NOT user-facing strings — leave them). Any user-facing string returning "Softball Mechanics" must be fixed.
  </action>
  <verify>
    <automated>cd /Users/abhishekhodavdekar/git/softball-mechanics && grep -rn "Softball Mechanics" src/ --include="*.ts" --include="*.tsx" | grep -v "//\|biomechanics\|PMC\|Source:\|worker\|yolo\|Purpose:"</automated>
  </verify>
  <done>
  The grep command returns 0 results (all user-facing "Softball Mechanics" strings replaced).
  Comments/scientific citations (flags.ts, angles.ts) are excluded by the grep filter and OK to leave.
  Both invite pages have a Diamond Mechanics wordmark above status content.
  Email HTML in auth.ts and feedback.ts says "Diamond Mechanics".
  src/lib/email.ts has been grepped and any "Softball Mechanics" strings replaced (or confirmed absent).
  Consent text says "baseball and softball mechanics analysis".
  Inngest client id is 'diamond-mechanics'.
  `npm run build` succeeds.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn "Softball Mechanics" src/app src/actions src/components src/inngest src/lib --include="*.ts" --include="*.tsx"` returns 0 user-facing string matches
2. `grep -n "Diamond Mechanics" src/app/(app)/layout.tsx` returns a match
3. `grep -n "Diamond Mechanics" src/app/(auth)/layout.tsx` returns a match
4. `grep -n "Diamond Mechanics" src/app/invite/accept/page.tsx` returns a match
5. `grep -n "Diamond Mechanics" src/app/invite/\[token\]/page.tsx` returns a match
6. `grep -n "diamond-mechanics" src/inngest/client.ts` returns a match
7. `npm run build` passes without errors
</verification>

<success_criteria>
- App shell nav: "Diamond Mechanics" wordmark (with optional diamond icon)
- Auth layout: "Diamond Mechanics" name + updated tagline
- Invite pages: both show brand name above status messages
- Email copy: both invite and feedback emails say "Diamond Mechanics"
- Consent text: updated to "baseball and softball mechanics analysis"
- Inngest client id: 'diamond-mechanics'
- Zero user-facing "Softball Mechanics" strings remaining in src/
</success_criteria>

<output>
After completion, create `.planning/phases/05-branding-and-marketing/05-03-SUMMARY.md` with:
- Files modified and specific strings changed
- Logo treatment decision (icon + wordmark vs. wordmark only)
- Any remaining "softball" references intentionally preserved (code comments/citations)
- Build status
</output>

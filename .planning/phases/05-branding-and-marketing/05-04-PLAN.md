---
phase: 05-branding-and-marketing
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/upload/page.tsx
  - src/app/(app)/review/[videoId]/page.tsx
  - src/app/(app)/roster/page.tsx
  - src/app/(app)/submissions/page.tsx
  - src/app/icon.svg
  - src/app/opengraph-image.png
autonomous: false
requirements: [BRAND-05]

must_haves:
  truths:
    - "Browser tab for Dashboard shows 'Dashboard | Diamond Mechanics'"
    - "Browser tab for Upload shows 'Upload | Diamond Mechanics'"
    - "Browser tab for Review shows 'Review | Diamond Mechanics'"
    - "Browser tab for Roster shows 'Roster | Diamond Mechanics'"
    - "Browser tab for Submissions shows 'Submissions | Diamond Mechanics'"
    - "Browser favicon is a Diamond Mechanics brand mark (not the default Next.js icon)"
    - "OG image file exists at src/app/opengraph-image.png (minimum: valid PNG accepted by Next.js build; full 1200x630 branded image required before production launch)"
  artifacts:
    - path: "src/app/icon.svg"
      provides: "Brand favicon — diamond shape or DM monogram"
    - path: "src/app/opengraph-image.png"
      provides: "Static OG image for social sharing, 1200x630px"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Dashboard page metadata export"
      exports: ["metadata"]
    - path: "src/app/(app)/upload/page.tsx"
      provides: "Upload page metadata export"
      exports: ["metadata"]
    - path: "src/app/(app)/review/[videoId]/page.tsx"
      provides: "Review page metadata export"
      exports: ["metadata"]
    - path: "src/app/(app)/roster/page.tsx"
      provides: "Roster page metadata export"
      exports: ["metadata"]
    - path: "src/app/(app)/submissions/page.tsx"
      provides: "Submissions page metadata export"
      exports: ["metadata"]
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "browser tab title"
      via: "metadata.title — uses template from root layout"
      pattern: "title.*Dashboard"
    - from: "src/app/icon.svg"
      to: "browser favicon"
      via: "Next.js App Router file convention auto-detection"
      pattern: "<svg"
---

<objective>
Add per-page metadata exports to all app routes so browser tabs show meaningful titles, and deliver the brand favicon and OG image assets. This completes the SEO/meta layer of the Diamond Mechanics rebrand.

Purpose: Coaches and athletes see clean, branded titles in browser tabs and bookmarks. The favicon replaces the default Next.js boilerplate. Social sharing of the landing page URL shows a branded preview card.
Output: Per-page metadata in 5 app routes; icon.svg favicon; opengraph-image.png static asset.
</objective>

<execution_context>
@/Users/abhishekhodavdekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abhishekhodavdekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-branding-and-marketing/05-01-SUMMARY.md
@.planning/phases/05-branding-and-marketing/05-03-SUMMARY.md

<interfaces>
<!-- Root layout metadata (set in Plan 01): -->
<!-- title.template = '%s | Diamond Mechanics' -->
<!-- Per-page title exports just need: export const metadata: Metadata = { title: 'Page Name' } -->
<!-- The template system automatically appends '| Diamond Mechanics' -->

<!-- Next.js App Router file conventions for assets: -->
<!-- src/app/icon.svg → auto-detected → <link rel="icon" type="image/svg+xml" ...> in <head> -->
<!-- src/app/opengraph-image.png → auto-detected → og:image meta tags for root route -->
<!-- No manual <link> tags or metadata.icons entries needed -->

<!-- Per-page metadata pattern (already established in root layout): -->
```typescript
import type { Metadata } from 'next'
export const metadata: Metadata = {
  title: 'Dashboard',  // renders: "Dashboard | Diamond Mechanics"
}
```

<!-- Pages to update and their titles: -->
<!-- dashboard/page.tsx → 'Dashboard' -->
<!-- upload/page.tsx → 'Upload' -->
<!-- review/[videoId]/page.tsx → 'Review' -->
<!-- roster/page.tsx → 'Roster' -->
<!-- submissions/page.tsx → 'Submissions' -->

<!-- NOTE: These pages are server components — adding a top-level metadata export is safe. -->
<!-- For review/[videoId]/page.tsx: even though it's a dynamic route, a static title 'Review' -->
<!-- is correct for v1. Dynamic per-video titles (e.g. "Review - @athlete name") are a future enhancement. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-page metadata to all app routes</name>
  <files>src/app/(app)/dashboard/page.tsx, src/app/(app)/upload/page.tsx, src/app/(app)/review/[videoId]/page.tsx, src/app/(app)/roster/page.tsx, src/app/(app)/submissions/page.tsx</files>
  <action>
Add `export const metadata: Metadata` to each app page. Add the import at the top of each file if not already present.

**Pattern for each file:**
```typescript
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'PAGE_TITLE',
  description: 'PAGE_DESCRIPTION',
}
```

**Page → title + description mapping:**
- dashboard/page.tsx → `title: 'Dashboard'`, `description: 'View and manage your athletes\' recent video submissions.'`
- upload/page.tsx → `title: 'Upload'`, `description: 'Upload a video clip for instant AI-powered mechanics analysis.'`
- review/[videoId]/page.tsx → `title: 'Review'`, `description: 'Review pose analysis, mechanics flags, and coaching feedback for this video.'`
- roster/page.tsx → `title: 'Roster'`, `description: 'Manage your athlete roster and send invitations.'`
- submissions/page.tsx → `title: 'Submissions'`, `description: 'Browse all video submissions from your athletes.'`

**Important:** Each page file already has a default export (the page component). Add the metadata export ABOVE the default export. Do NOT change the component implementation — this is an additive change only.

**Check before adding:** If a page already has `import type { Metadata } from 'next'` or `export const metadata`, do not duplicate — update instead.

After updating all 5 files, verify:
```bash
grep -l "export const metadata" src/app/(app)/dashboard/page.tsx src/app/(app)/upload/page.tsx "src/app/(app)/review/[videoId]/page.tsx" src/app/(app)/roster/page.tsx src/app/(app)/submissions/page.tsx
```
All 5 files should be listed.
  </action>
  <verify>
    <automated>cd /Users/abhishekhodavdekar/git/softball-mechanics && grep -rn "export const metadata" src/app/\(app\)/</automated>
  </verify>
  <done>All 5 app pages have `export const metadata` export with both `title` and `description` fields. `npm run build` succeeds without TypeScript errors. No existing component logic modified.</done>
</task>

<task type="auto">
  <name>Task 2: Create brand favicon (icon.svg) and OG image (opengraph-image.png)</name>
  <files>src/app/icon.svg, src/app/opengraph-image.png</files>
  <action>
**icon.svg — Diamond Mechanics favicon:**
Create `src/app/icon.svg` with a diamond shape (rhombus) in Diamond blue. This is a clean geometric mark — no text needed at favicon size.

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <polygon points="16,2 30,16 16,30 2,16" fill="#2563eb"/>
</svg>
```

Use #2563eb (≈ oklch(0.546 0.245 262.881), Diamond blue). SVG favicons use hex color directly (not CSS vars).

Note: Next.js App Router auto-detects `src/app/icon.svg` and injects `<link rel="icon" type="image/svg+xml">` into `<head>` automatically. No manual <link> tag needed in layout.tsx.

**opengraph-image.png — Static OG image:**
Create `src/app/opengraph-image.png` as a 1200×630 PNG with the Diamond Mechanics brand identity. Since generating a binary PNG requires tooling, use one of these approaches in order of preference:

**Option A (preferred): Generate with a Node.js script:**
```bash
# Use sharp (already likely in node_modules as a Next.js transitive dep) or create a minimal SVG-to-PNG:
# If sharp is not available, use the Canvas API or inline SVG approach
node -e "
const fs = require('fs');
// Create a minimal SVG and embed as data URI
const svg = \`<svg xmlns='http://www.w3.org/2000/svg' width='1200' height='630'>
  <rect width='1200' height='630' fill='#f8fafc'/>
  <polygon points='600,80 820,315 600,550 380,315' fill='#2563eb'/>
  <text x='600' y='430' font-family='sans-serif' font-size='72' font-weight='bold' fill='#1e293b' text-anchor='middle'>Diamond Mechanics</text>
  <text x='600' y='510' font-family='sans-serif' font-size='32' fill='#64748b' text-anchor='middle'>AI-powered mechanics coaching for baseball and softball</text>
</svg>\`;
// Check if we can write PNG, otherwise create a placeholder text file and note it
console.log('OG SVG ready — convert to PNG manually or via CLI');
"
```

**Option B (fallback): Create an SVG file and reference it:**
If generating a PNG is not feasible without additional tooling, create `src/app/opengraph-image.png` as a minimal valid placeholder PNG using raw base64. The minimal 1x1 white PNG base64 is:
```
iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwADhQGAWjR9awAAAABJRU5ErkJggg==
```
Write this as binary. Note in the SUMMARY that a proper OG image should be created manually before launch.

**Recommended approach for executor:** Use the `sharp` package if available in node_modules (it's a common Next.js transitive dep):
```bash
node -e "
try {
  const sharp = require('sharp');
  const { createCanvas } = require('canvas'); // may not be available
} catch(e) { console.log('sharp/canvas not available:', e.message); }
"
```

If neither sharp nor canvas is available, write a minimal valid PNG byte array directly using Node.js Buffer:
```bash
node -e "
const fs = require('fs');
// Minimal valid 1x1 white PNG
const png = Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwADhQGAWjR9awAAAABJRU5ErkJggg==', 'base64');
fs.writeFileSync('src/app/opengraph-image.png', png);
console.log('Placeholder OG image written');
"
```

**IMPORTANT:** Document in the SUMMARY whether a real 1200×630 branded OG image was generated or a placeholder was used. The metadata.openGraph.images config in layout.tsx (set in Plan 01) already references '/opengraph-image.png' — the file just needs to exist for Next.js to serve it.

**WARNING — Duplicate og:image risk:** Plan 01 added an explicit `openGraph.images` array to `src/app/layout.tsx`, AND placing `src/app/opengraph-image.png` here (Next.js file convention) causes Next.js to auto-inject a second `og:image` tag. This may produce duplicate `og:image` meta tags. After placing the file, verify with `npm run build && grep -r "og:image" .next/server/app/index.html` (or check DevTools on the root route). If duplicates appear, pick ONE mechanism:
- **Option A (file convention only):** Remove the `openGraph.images` array from `src/app/layout.tsx` and keep the file in `src/app/`.
- **Option B (explicit metadata only):** Move the file to `public/opengraph-image.png` and keep the `openGraph.images` array in layout.tsx pointing to `/opengraph-image.png`.
Document the chosen mechanism in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/abhishekhodavdekar/git/softball-mechanics && ls -la src/app/icon.svg src/app/opengraph-image.png 2>&1</automated>
  </verify>
  <done>
  - `src/app/icon.svg` exists and contains a `<polygon>` or `<path>` element with fill="#2563eb"
  - `src/app/opengraph-image.png` exists (may be placeholder or real branded image)
  - `npm run build` succeeds — Next.js detects both assets and injects appropriate metadata tags
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Per-page metadata on all app routes (Dashboard, Upload, Review, Roster, Submissions), Diamond Mechanics favicon (icon.svg), and OG image (opengraph-image.png).</what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Visit each page and check the browser tab title:
       - http://localhost:3000/dashboard → tab should say "Dashboard | Diamond Mechanics"
       - http://localhost:3000/upload → "Upload | Diamond Mechanics"
       - http://localhost:3000/roster → "Roster | Diamond Mechanics"
       - http://localhost:3000/submissions → "Submissions | Diamond Mechanics"
       - http://localhost:3000/review/[any-video-id] → "Review | Diamond Mechanics"
    3. Check the browser tab favicon (should show a blue diamond shape, not the Next.js boilerplate)
    4. Visit http://localhost:3000 (landing page) — favicon should also be the diamond
    5. In browser DevTools → Elements → `<head>`, verify:
       - `<link rel="icon" ...>` points to the diamond SVG
       - `<meta property="og:image" ...>` is present (from root layout metadata)
    6. Optional: Use https://opengraph.xyz or paste the URL into a Slack/Discord message to preview the OG card (may require deployed URL)
  </how-to-verify>
  <resume-signal>Type "approved" if all browser tab titles are correct and favicon shows the diamond mark, or describe specific issues.</resume-signal>
</task>

</tasks>

<verification>
After all tasks and checkpoint approved:
1. `grep -rn "export const metadata" src/app/(app)/` returns 5 results (one per page)
2. `ls src/app/icon.svg` — file exists
3. `ls src/app/opengraph-image.png` — file exists
4. `grep "2563eb" src/app/icon.svg` returns a match (brand blue in favicon)
5. `npm run build` succeeds
</verification>

<success_criteria>
- All 5 app pages have metadata export with correct title
- Browser tabs show "[Page] | Diamond Mechanics" format
- favicon is a Diamond Mechanics brand mark (not Next.js default)
- opengraph-image.png exists and is referenced in root layout metadata
- Human-verify checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-branding-and-marketing/05-04-SUMMARY.md` with:
- All 5 pages updated with metadata
- Favicon SVG design used
- OG image: real branded image or placeholder (note if placeholder)
- Checkpoint approval status
- Phase 5 complete confirmation
</output>

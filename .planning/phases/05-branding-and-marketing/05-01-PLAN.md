---
phase: 05-branding-and-marketing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-05]

must_haves:
  truths:
    - "All buttons, focus rings, and primary interactive elements use Diamond blue, not near-black"
    - "Browser tab shows 'Diamond Mechanics' (not 'Create Next App')"
    - "Root layout metadata has correct title template and description mentioning baseball and softball"
    - "OG metadata configured for the landing page"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand color tokens: --primary, --primary-foreground, --ring, --sidebar-primary, --sidebar-ring"
      contains: "oklch(0.546 0.245 262.881)"
    - path: "src/app/layout.tsx"
      provides: "Root layout metadata with title template and OG config"
      exports: ["metadata"]
  key_links:
    - from: "src/app/globals.css"
      to: "shadcn Button, Input, Link components"
      via: "--primary token cascade via @theme inline"
      pattern: "--primary: oklch"
    - from: "src/app/layout.tsx"
      to: "browser tab title"
      via: "metadata.title.template"
      pattern: "template.*Diamond Mechanics"
---

<objective>
Apply Diamond Mechanics brand color tokens to globals.css and update the root layout metadata. This is the foundation plan — it cascades brand blue across every shadcn component before any other UI work begins, and sets the correct site-wide title template so per-page metadata renders correctly.

Purpose: Every subsequent plan builds UI against the already-branded token system. The brand color must exist before landing page, auth, and nav work begins.
Output: Updated globals.css with Diamond blue tokens; updated layout.tsx with Diamond Mechanics metadata.
</objective>

<execution_context>
@/Users/abhishekhodavdekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abhishekhodavdekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current globals.css :root block for reference (partial) -->
<!-- Executor must update only the token values listed below; preserve all others -->

From src/app/globals.css — current :root tokens to update:
```css
--primary: oklch(0.205 0 0);           /* near-black — replace with Diamond blue -->
--primary-foreground: oklch(0.985 0 0); /* white — keep as-is -->
--ring: oklch(0.708 0 0);               /* gray — replace with Diamond blue -->
--sidebar-primary: oklch(0.205 0 0);    /* near-black — replace with Diamond blue -->
--sidebar-ring: oklch(0.708 0 0);       /* gray — replace with Diamond blue -->
```

From src/app/globals.css — .dark block tokens to update:
```css
/* .dark --sidebar-primary is already oklch(0.488 0.243 264.376) — a dark-mode blue. Update --primary and --ring too. */
--primary: oklch(0.922 0 0);           /* light gray — replace with a lighter blue for dark mode -->
--ring: oklch(0.556 0 0);              /* mid-gray — replace with Diamond blue -->
```

From src/app/layout.tsx — current metadata export:
```typescript
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS brand color tokens in globals.css</name>
  <files>src/app/globals.css</files>
  <action>
In src/app/globals.css, update the :root block and .dark block with Diamond Mechanics brand colors. Use oklch values throughout (do NOT use hex values directly in CSS vars — match the existing oklch format).

**:root block — update these tokens ONLY (preserve all other :root tokens):**
```css
--primary: oklch(0.546 0.245 262.881);         /* Diamond blue — royal/cobalt ≈ #2563eb */
--primary-foreground: oklch(0.985 0 0);         /* white text on blue — already correct, no change needed */
--ring: oklch(0.546 0.245 262.881);             /* focus ring matches brand blue */
--sidebar-primary: oklch(0.546 0.245 262.881);  /* sidebar active nav item: brand blue */
--sidebar-primary-foreground: oklch(0.985 0 0); /* white text on blue sidebar active — already correct */
--sidebar-ring: oklch(0.546 0.245 262.881);     /* sidebar focus ring: brand blue */
```

**`.dark` block — update these tokens ONLY:**
```css
--primary: oklch(0.65 0.20 262.881);            /* lighter blue for dark mode — readable on dark background */
--primary-foreground: oklch(0.145 0 0);         /* dark text on light-blue dark-mode primary */
--ring: oklch(0.546 0.245 262.881);             /* focus ring: brand blue (readable against dark bg) */
/* sidebar-primary in .dark is already oklch(0.488 0.243 264.376) — keep as-is, it's already dark-mode blue */
```

Do NOT change: --background, --foreground, --card, --popover, --secondary, --muted, --accent, --destructive, --border, --input, --chart-* tokens, or any other tokens not listed above.

After updating, verify no duplicate property declarations are present in the same block.
  </action>
  <verify>
    <automated>cd /Users/abhishekhodavdekar/git/softball-mechanics && grep -n "0.546 0.245 262.881" src/app/globals.css</automated>
  </verify>
  <done>The string "0.546 0.245 262.881" appears at least 3 times in globals.css (for --primary, --ring, --sidebar-primary in :root block). The .dark block --primary is updated to a lighter blue value. No hex color values introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Update root layout metadata to Diamond Mechanics</name>
  <files>src/app/layout.tsx</files>
  <action>
In src/app/layout.tsx, replace the existing `metadata` export with the Diamond Mechanics brand metadata including title template, description, and openGraph config:

```typescript
export const metadata: Metadata = {
  title: {
    default: 'Diamond Mechanics',
    template: '%s | Diamond Mechanics',
  },
  description: 'AI-powered mechanics coaching for baseball and softball coaches. Upload video, get instant pose analysis, and deliver targeted feedback to your athletes.',
  openGraph: {
    title: 'Diamond Mechanics',
    description: 'AI-powered mechanics coaching for baseball and softball coaches.',
    type: 'website',
    images: [{ url: '/opengraph-image.png', width: 1200, height: 630, alt: 'Diamond Mechanics' }],
  },
};
```

Leave all other layout.tsx content unchanged (font loading, Providers wrapper, html/body structure).
  </action>
  <verify>
    <automated>cd /Users/abhishekhodavdekar/git/softball-mechanics && grep -n "Diamond Mechanics" src/app/layout.tsx</automated>
  </verify>
  <done>"Diamond Mechanics" appears in src/app/layout.tsx metadata export. The title uses a template object (not a string). The description mentions "baseball and softball". The metadata export includes openGraph with the OG image reference. "Create Next App" is gone.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -n "0.546 0.245 262.881" src/app/globals.css` returns at least 3 matches
2. `grep -n "Create Next App\|Generated by create" src/app/layout.tsx` returns 0 matches
3. `grep -n "Diamond Mechanics" src/app/layout.tsx` returns at least 3 matches (title, og:title, og:description)
4. `npm run build` passes without type errors (layout.tsx Metadata type is correctly typed)
</verification>

<success_criteria>
- globals.css :root --primary is oklch(0.546 0.245 262.881)
- globals.css :root --ring is oklch(0.546 0.245 262.881)
- globals.css :root --sidebar-primary is oklch(0.546 0.245 262.881)
- layout.tsx exports metadata with title.template = '%s | Diamond Mechanics'
- layout.tsx description mentions "baseball and softball"
- layout.tsx openGraph.images references '/opengraph-image.png'
</success_criteria>

<output>
After completion, create `.planning/phases/05-branding-and-marketing/05-01-SUMMARY.md` with:
- Files modified
- Token values applied
- Any deviations from plan
</output>

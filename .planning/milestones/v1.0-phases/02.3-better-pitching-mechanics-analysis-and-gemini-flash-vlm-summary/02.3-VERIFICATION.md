---
phase: 02.3-better-pitching-mechanics-analysis-and-gemini-flash-vlm-summary
verified: 2026-03-02T07:30:00Z
status: human_needed
score: 8/8 automated must-haves verified
re_verification: false
human_verification:
  - test: "Open a pitching video in the review page and confirm sidebar labels are correct"
    expected: "Current Frame section shows 'Hip Drive' (not 'Hip Rotation') with range 35-60 deg, and 'Arm Slot' (not 'Elbow Slot') with range 60-120 deg. Shoulder Tilt still shows -20 to 20 deg. Motion badge shows 'Pitching Analysis' in purple."
    why_human: "Label rendering depends on motionType value flowing from DB through server component to ReviewPageClient at runtime — cannot confirm correct DB value or rendering path without a live browser session."
  - test: "On the same pitching video, click 'Get AI Commentary' in the Session Summary section"
    expected: "Button appears below 'Needs Work' / 'Looking Good' lists only when analysis is complete and a contact frame exists. Clicking shows 'Getting AI Commentary...' loading state for 2-4 seconds, then displays 2-3 sentences of coach-to-athlete commentary (not bullet points, not a report style). 'Regenerate' link appears below the text."
    why_human: "Requires GEMINI_API_KEY set in .env.local, a real video with a complete analysis, and a browser to observe the loading state transition and Gemini response quality."
  - test: "Reload the review page after generating commentary and confirm it persists"
    expected: "Commentary text loads immediately on page reload without clicking 'Get AI Commentary' again. The text matches what Gemini returned in the previous session."
    why_human: "Verifies the DB write in vlm-eval and the SELECT in usePoseAnalysis hook both work together at runtime — requires a Supabase-connected environment."
  - test: "Open a hitting video and confirm existing labels and ranges are unchanged"
    expected: "Current Frame section shows 'Hip Rotation' with range 75-105 deg and 'Elbow Slot' with range 70-110 deg. New pitching flags (Arm Circle, Stride Off Power Line) do not appear in the Mechanics Issues panel."
    why_human: "Regression check — requires a real hitting video and browser session."
  - test: "Click 'Regenerate' on an existing commentary to overwrite it"
    expected: "A new 2-3 sentence Gemini response replaces the previous text. The new text is also persisted to DB (confirmed by page reload showing the new text)."
    why_human: "Requires live Gemini API call and Supabase DB write verification."
---

# Phase 02.3: Better Pitching Mechanics Analysis and Gemini Flash VLM Summary — Verification Report

**Phase Goal:** Add two new pitching mechanics flags (Arm Circle bent elbow, Stride Off Power Line), pitching-specific ideal ranges and sidebar label rename, and Gemini Flash VLM commentary wired into the Session Summary section with DB persistence.
**Verified:** 2026-03-02T07:30:00Z
**Status:** human_needed — all automated checks pass; 5 items need live browser/API verification
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | `flagPitchingMechanics()` returns an Arm Circle (Bent Elbow) flag when elbow angle < 120 deg on pitching frames | VERIFIED | `detectArmCircleBentElbow` defined at flags.ts:157, called from `flagPitchingMechanics` at line 321. Uses `angleBetweenThreePoints(shoulder, elbow, wrist)` with `BENT_THRESHOLD = 120`. 14 Vitest tests passing including bent-arm trigger and visibility gating. |
| 2  | `flagPitchingMechanics()` returns a Stride Off Power Line flag when stride ankle deviates significantly from drive ankle in the depth axis | VERIFIED | `detectStrideOffPowerLine` defined at flags.ts:198, called from `flagPitchingMechanics` at line 322. Uses `Math.abs(strideAnkle.z - driveAnkle.z) > 0.15` with `[Experimental]` suffix. Test coverage confirmed in `flags.pitching.test.ts`. |
| 3  | `PITCHING_IDEAL_RANGES` is exported from `flags.ts` with `elbowSlot`, `shoulderTilt`, and `hipDrive` ranges | VERIFIED | Exported at flags.ts:43-47. Values: `elbowSlot {min:60, max:120}`, `shoulderTilt {min:-20, max:20}`, `hipDrive {min:35, max:60}`. 3 Vitest tests confirm exact values. |
| 4  | `SummaryFrame` interface includes optional `isContact` and `timestampMs` fields | VERIFIED | `AnalysisSummary.tsx:15-16` — both fields present with comments. `markContactFrame()` in `usePoseAnalysis` sets `isContact: true` on the peak hip-rotation frame. |
| 5  | All new flag functions gate on `FLAG_CONFIDENCE_THRESHOLD` (0.70) per-landmark visibility | VERIFIED | `detectArmCircleBentElbow` checks visibility on all 3 arm landmarks (flags.ts:163-165). `detectStrideOffPowerLine` checks both ankle landmarks (flags.ts:202-203). Test coverage confirms visibility gating in `flags.pitching.test.ts`. |
| 6  | When `motionType === 'pitching'`, `MechanicsSidebar` shows "Hip Drive" / "Arm Slot" with `PITCHING_IDEAL_RANGES` values | VERIFIED (code) / HUMAN NEEDED (runtime) | `isPitching`, `hipLabel`, `elbowLabel`, `hipRanges`, `elbowRanges` derived at MechanicsSidebar.tsx:125-129. AngleRow calls use these vars at lines 205-214. Import of `PITCHING_IDEAL_RANGES` confirmed at line 4. Runtime label rendering requires human verification. |
| 7  | "Get AI Commentary" button appears in Session Summary and calls `/api/analysis/vlm-eval`, returning persisted coach-to-athlete commentary | VERIFIED (code) / HUMAN NEEDED (runtime) | Button gated on `analysisStatus in [complete, low_confidence] && frames.some(f => f.isContact)` (AnalysisSummary.tsx:187-188). `handleRequestCommentary` in ReviewPageClient.tsx:115 extracts contact frame, POSTs to `/api/analysis/vlm-eval` with `base64Frame`, `motionType`, `videoId`. Requires live API call to verify Gemini response quality. |
| 8  | Commentary persists to `video_analyses.vlm_summary` and loads on page init | VERIFIED (code) / HUMAN NEEDED (runtime) | Migration `007_vlm_summary.sql` adds `ALTER TABLE video_analyses ADD COLUMN IF NOT EXISTS vlm_summary TEXT`. `usePoseAnalysis` SELECTs `vlm_summary` at line 248, sets `vlmSummary` state at line 256, returned at line 301. ReviewPageClient syncs to `vlmCommentary` state via `useEffect([vlmSummary])` at lines 52-54. DB persistence requires live Supabase verification. |

**Automated Score:** 8/8 truths have full code evidence. 5 of 8 also require human runtime verification.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/pose/flags.ts` | `detectArmCircleBentElbow`, `detectStrideOffPowerLine`, `PITCHING_IDEAL_RANGES` | VERIFIED | All present and substantive. Both detectors are private functions called from `flagPitchingMechanics`. Export confirmed at line 43. File is 344 lines — not a stub. |
| `src/components/review/AnalysisSummary.tsx` | `SummaryFrame` extended with `isContact?` and `timestampMs?`; VLM button + commentary display | VERIFIED | `SummaryFrame` at lines 8-17 includes both fields. Full VLM section at lines 186-226 includes button, loading state, commentary display, and Regenerate link. 229 lines total — substantive. |
| `src/components/review/MechanicsSidebar.tsx` | Motion-type-aware `AngleRow` calls using `hipLabel`, `elbowLabel`, `hipRanges`, `elbowRanges`; VLM pass-through props | VERIFIED | `isPitching` derived at line 125; conditional vars at 126-129; `AngleRow` calls at 205-214 use them. VLM props accepted (lines 24-26) and passed to `AnalysisSummary` (lines 276-279). 300 lines — substantive. |
| `src/hooks/usePoseAnalysis.ts` | SELECT includes `vlm_summary`; `vlmSummary: string | null` in return value | VERIFIED | SELECT extended at line 248. `vlmSummary` state declared at line 46, set at line 256, returned at line 301. Interface updated at lines 31-33. |
| `src/app/api/analysis/vlm-eval/route.ts` | Production route: `videoId` UUID param, coach-to-athlete prompt, non-fatal DB write | VERIFIED | `videoId: z.string().uuid()` in schema at line 17. Coach-to-athlete prompt at lines 45-50. Non-fatal `try/catch` DB write at lines 68-75. Prototype header comment replaced with production comment at lines 1-7. |
| `supabase/migrations/007_vlm_summary.sql` | `ALTER TABLE video_analyses ADD COLUMN IF NOT EXISTS vlm_summary TEXT` | VERIFIED | File exists at expected path. Content matches exactly: `ALTER TABLE video_analyses ADD COLUMN IF NOT EXISTS vlm_summary TEXT`. |
| `src/lib/pose/__tests__/flags.pitching.test.ts` | 14 Vitest TDD tests for new pitching flags (bonus artifact) | VERIFIED | 213 lines. Tests cover: `PITCHING_IDEAL_RANGES` values (3 tests), `detectArmCircleBentElbow` trigger/no-trigger/visibility/confidence (5 tests), `detectStrideOffPowerLine` trigger/no-trigger/visibility/confidence (4 tests), hitting isolation (2 tests). |
| `vitest.config.ts` | Vitest config with `@/` path alias (bonus artifact) | VERIFIED | Created during Plan 01 TDD setup as first test framework in project. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `detectArmCircleBentElbow` | `flagPitchingMechanics()` | Direct call | WIRED | flags.ts:321 `flags.push(...detectArmCircleBentElbow(landmarks))` |
| `detectStrideOffPowerLine` | `flagPitchingMechanics()` | Direct call | WIRED | flags.ts:322 `flags.push(...detectStrideOffPowerLine(landmarks))` |
| `PITCHING_IDEAL_RANGES` | `MechanicsSidebar.tsx` | Import | WIRED | MechanicsSidebar.tsx:4 imports from `@/lib/pose/flags`; used at lines 128-129 for `hipRanges` and `elbowRanges` |
| `SummaryFrame.isContact` | `AnalysisSummary.tsx` VLM gate | `frames.some(f => f.isContact)` | WIRED | AnalysisSummary.tsx:188 gates VLM button section on this. `markContactFrame()` in usePoseAnalysis sets the field at lines 77, 212. |
| `SummaryFrame.timestampMs` | `ReviewPageClient.handleRequestCommentary` | `contactFrame.timestampMs` | WIRED | ReviewPageClient.tsx:117 reads `contactFrame.timestampMs`; passes to `extractContactFrame` at line 120. |
| `ReviewPageClient.handleRequestCommentary` | `/api/analysis/vlm-eval` | `fetch('/api/analysis/vlm-eval', { method: 'POST' })` | WIRED | ReviewPageClient.tsx:122. Body includes `base64Frame`, `motionType`, `videoId`. Response `description` extracted at line 133. |
| `MechanicsSidebar` | `AnalysisSummary` (VLM props) | Pass-through props | WIRED | Props `vlmCommentary`, `onRequestCommentary`, `isCommentaryLoading`, `analysisStatus` all accepted by MechanicsSidebar (lines 24-26, 102, 116-118) and forwarded to `<AnalysisSummary>` (lines 276-279). |
| `usePoseAnalysis.vlmSummary` | `ReviewPageClient.vlmCommentary` state | `useEffect([vlmSummary])` | WIRED | ReviewPageClient.tsx:52-54: `useEffect(() => { if (vlmSummary) setVlmCommentary(vlmSummary) }, [vlmSummary])` |
| `vlm-eval route` | `video_analyses.vlm_summary` | `supabase.from('video_analyses').update({ vlm_summary: description })` | WIRED | route.ts:69-73. Wrapped in try/catch — non-fatal per plan design. |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PITCH-FLAG-01 | 02.3-01 | Arm Circle (Bent Elbow) pitching flag | SATISFIED | `detectArmCircleBentElbow` in flags.ts:157-182. Gated on 0.70 visibility. 5 tests. Called from `flagPitchingMechanics`. |
| PITCH-FLAG-02 | 02.3-01 | Stride Off Power Line pitching flag | SATISFIED | `detectStrideOffPowerLine` in flags.ts:198-220. Uses z-axis depth delta > 0.15 threshold. `[Experimental]` label. 4 tests. Called from `flagPitchingMechanics`. |
| PITCH-RANGES-01 | 02.3-01 | Pitching-specific ideal angle ranges | SATISFIED | `PITCHING_IDEAL_RANGES` exported at flags.ts:43-47. Values from PMC11969493 literature. 3 tests confirm exact values. |
| PITCH-LABELS-01 | 02.3-02 | Motion-type-aware sidebar labels (Hip Drive / Arm Slot) | SATISFIED (code) | MechanicsSidebar.tsx:125-129: `isPitching` derived, `hipLabel`/`elbowLabel`/`hipRanges`/`elbowRanges` conditionals. Requires human runtime check. |
| VLM-COMMENTARY-01 | 02.3-03 | "Get AI Commentary" button with Gemini commentary display, loading state, Regenerate option | SATISFIED (code) | AnalysisSummary.tsx:186-226. Full button/text/Regenerate UI. Gate on `analysisStatus` and `isContact`. Handler in ReviewPageClient extracts frame, calls API. Requires live human verification. |
| VLM-DB-01 | 02.3-03 | DB persistence of vlm_summary; loads on page init without re-calling Gemini | SATISFIED (code) | Migration 007 adds column. `usePoseAnalysis` SELECTs and returns `vlmSummary`. `useEffect` syncs to component state. Requires live DB verification. |

**REQUIREMENTS.md traceability note:** The 6 phase-02.3 requirement IDs (PITCH-FLAG-*, PITCH-RANGES-01, PITCH-LABELS-01, VLM-*) are phase-internal IDs defined only in PLAN frontmatter. They do NOT appear in `.planning/REQUIREMENTS.md`, which tracks only the 22 v1 core requirements (AUTH, VID, ANN, AI, FEED, ROST series). This is expected: phase 02.3 is an INSERTED phase with its own internal requirement namespace. No orphaned requirements found. No REQUIREMENTS.md traceability table update is needed unless the team decides to add these as AI-series requirements in a future pass.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

Scanned all 6 phase-modified source files for TODO/FIXME/placeholder comments, empty implementations, and stub return values. No anti-patterns detected. The prototype header comment in `vlm-eval/route.ts` was correctly replaced with a production comment block in commit `61888d7`.

---

### Human Verification Required

#### 1. Pitching sidebar labels render correctly at runtime

**Test:** Open a video that was uploaded with `motion_type = 'pitching'` in the review page.
**Expected:** The Current Frame section in the sidebar shows "Hip Drive" (not "Hip Rotation") with ideal range 35-60 deg, and "Arm Slot" (not "Elbow Slot") with range 60-120 deg. "Shoulder Tilt" remains with range -20 to 20 deg. A purple "Pitching Analysis" motion badge appears.
**Why human:** The `motionType` prop flows from Supabase `videos.motion_type` through the server component at `/review/[videoId]/page.tsx` to `ReviewPageClient`. Confirming the DB value is correctly read and propagated requires a live browser session.

#### 2. "Get AI Commentary" button appears and triggers Gemini commentary

**Test:** On a pitching video with completed analysis, scroll the sidebar to the Session Summary section. Confirm the "AI Commentary" section is visible below "Needs Work" / "Looking Good". Click "Get AI Commentary".
**Expected:** Button shows "Getting AI Commentary..." loading state for 2-4 seconds. Gemini returns 2-3 sentences in direct coach-to-athlete voice (e.g., "Your arm circle..."). No bullet points. Commentary text appears in the sidebar.
**Why human:** Requires `GEMINI_API_KEY` in `.env.local`, a live Supabase connection, a real video with `isContact: true` on at least one frame, and observation of the UI state transition.

#### 3. Commentary persists across page reload

**Test:** After generating commentary in test 2, reload the review page for the same video.
**Expected:** Commentary loads immediately without clicking the button again. Text matches what Gemini returned.
**Why human:** Verifies both the DB write in `vlm-eval/route.ts` and the `SELECT vlm_summary` in `usePoseAnalysis` are working together at runtime against a real Supabase instance.

#### 4. Hitting video is unaffected (regression check)

**Test:** Open a video uploaded with `motion_type = 'hitting'` in the review page.
**Expected:** "Hip Rotation" with range 75-105 deg and "Elbow Slot" with range 70-110 deg are shown. No "Arm Circle (Bent Elbow)" or "Stride Off Power Line" flags appear in the Mechanics Issues panel.
**Why human:** Regression check for the motion-type dispatch in `flagMechanics()` — confirmed at code level but runtime test needed.

#### 5. Regenerate overwrites existing commentary

**Test:** With existing commentary visible, click "Regenerate".
**Expected:** "Regenerating..." loading state shows briefly. New 2-3 sentence response appears, replacing the previous text. Page reload confirms the new text is persisted.
**Why human:** Tests the overwrite path in both the DB (same `update` call) and UI state management.

---

### Gaps Summary

No gaps found. All 8 automated must-haves are verified with full code evidence across three levels (exists, substantive, wired). TypeScript compilation exits with 0 errors. All commits documented in SUMMARY files are present in the git log (`21037dd`, `4722c7d`, `08383ec`, `5d3e85e`, `61888d7`, `24514a8`).

The 5 human verification items above are standard runtime checks that cannot be confirmed programmatically (live API keys, Supabase connections, browser rendering). They do not indicate code gaps — the wiring is complete.

---

_Verified: 2026-03-02T07:30:00Z_
_Verifier: Claude (gsd-verifier)_

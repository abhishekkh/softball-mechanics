---
phase: 02.2-ai-mechanics-analysis-and-model-evaluation
plan: "01"
subsystem: database
tags: [supabase, postgres, typescript, motion-type]

# Dependency graph
requires:
  - phase: 02-ai-pose-analysis
    provides: analysis.ts type contracts, video_analyses table, DB schema baseline
provides:
  - MotionType TypeScript type ('hitting' | 'pitching' | 'unknown') exported from src/types/analysis.ts
  - motion_type column on videos table with CHECK constraint and DEFAULT 'unknown'
  - Optional motionType field on AnalysisPayload interface
affects:
  - 02.2-02 (upload flow motion-type tagging)
  - 02.2-03 (motion-specific flag dispatch)
  - 02.2-04 (VLM prototype)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "MotionType as single source of truth — DB column + TypeScript type always in sync"
    - "Optional field pattern on AnalysisPayload — backward compatible extension without breaking existing consumers"

key-files:
  created:
    - supabase/migrations/006_motion_type.sql
  modified:
    - src/types/analysis.ts

key-decisions:
  - "MotionType is a video-level property (not per-frame) — set once at upload time, not on FrameAnalysis"
  - "CHECK constraint includes 'unknown' — makes untagged videos explicitly representable without NULL semantics"
  - "DEFAULT 'unknown' — all pre-Phase-2.2 rows remain valid without a backfill migration"

patterns-established:
  - "Phase-level type contracts live in src/types/analysis.ts — all producers and consumers import from this single file"

requirements-completed: [MOTION-01]

# Metrics
duration: 5min
completed: 2026-03-01
---

# Phase 2.2 Plan 01: Motion Type Foundation Summary

**Added MotionType DB column (videos.motion_type) and TypeScript type contract enabling motion-specific analysis dispatch in Plans 02-04**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-01T05:39:21Z
- **Completed:** 2026-03-01T05:44:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Created `supabase/migrations/006_motion_type.sql` — ALTER TABLE adding motion_type TEXT with CHECK constraint and DEFAULT 'unknown'
- Exported `MotionType = 'hitting' | 'pitching' | 'unknown'` from src/types/analysis.ts
- Added optional `motionType?: MotionType` to AnalysisPayload interface
- TypeScript compiles clean — zero breaking changes to existing consumers (flags.ts, usePoseAnalysis.ts, presign route)

## Task Commits

Each task was committed atomically:

1. **Task 1: Supabase migration — add motion_type column to videos table** - `fc74494` (feat)
2. **Task 2: Add MotionType type and update AnalysisPayload in analysis.ts** - `6b76b94` (feat)

## Files Created/Modified
- `supabase/migrations/006_motion_type.sql` - ALTER TABLE videos ADD COLUMN motion_type with CHECK constraint and DEFAULT 'unknown'
- `src/types/analysis.ts` - Added MotionType type export and optional motionType field on AnalysisPayload

## Decisions Made
- MotionType is a video-level property, not per-frame — kept off FrameAnalysis per RESEARCH.md anti-pattern note
- CHECK constraint includes 'unknown' as an explicit valid state rather than using NULL for untagged videos — cleaner SQL semantics
- DEFAULT 'unknown' ensures all existing rows remain valid without a data backfill

## Deviations from Plan

None - plan executed exactly as written.

Note: `npx supabase db push --local` failed (no local Supabase instance running) — this was expected and documented as acceptable in the plan. The migration file is the primary artifact; developer applies it to the live DB via `npx supabase db push` or Supabase dashboard.

## Issues Encountered
- No local Supabase instance available for db push verification. Migration file is syntactically valid SQL and will be applied to live DB by developer.

## User Setup Required

**Developer action required:** Apply the migration to the live Supabase database:

```bash
npx supabase db push
```

Or apply manually via Supabase dashboard SQL editor using the content of `supabase/migrations/006_motion_type.sql`.

## Next Phase Readiness
- MotionType type is available for import — Plans 02, 03, and 04 can proceed
- videos.motion_type column defined — upload flow (Plan 02) can store motion type at upload time
- AnalysisPayload.motionType field available — analysis pipeline (Plan 03) can pass motion type to flag dispatch

---
*Phase: 02.2-ai-mechanics-analysis-and-model-evaluation*
*Completed: 2026-03-01*

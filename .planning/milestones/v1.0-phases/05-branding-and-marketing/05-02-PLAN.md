---
phase: 05-branding-and-marketing
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/page.tsx
autonomous: false
requirements: [BRAND-03]

must_haves:
  truths:
    - "Visiting the root URL shows a full landing page with hero, feature highlights, and how-it-works sections"
    - "Landing page hero has app name, tagline, and 'Start free' CTA button linking to /signup"
    - "Landing page has a feedback link for early coaches to submit feedback"
    - "Landing page copy mentions both baseball and softball coaches — not softball-only"
    - "Landing page has its own minimal header (logo + Sign in link) since it is outside the (app) route group"
    - "No screenshot, demo video, mockup, pricing section, testimonials, or email capture form"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Public marketing landing page as React Server Component"
      contains: "Start free"
  key_links:
    - from: "src/app/page.tsx"
      to: "/signup"
      via: "Start free CTA <a> tag"
      pattern: "href.*signup"
    - from: "src/app/page.tsx"
      to: "/login"
      via: "Sign in link in header"
      pattern: "href.*login"
---

<objective>
Build the Diamond Mechanics public landing page at the root URL. This replaces the "Softball Mechanics — coming soon" stub with a full-featured marketing page: hero section, 4 feature highlights, a 3-step how-it-works flow, and a feedback link. The page is a React Server Component (no interactivity needed) and renders outside the (app) auth shell, so it includes its own standalone header.

Purpose: First public face of Diamond Mechanics — converts curious coaches to signups.
Output: src/app/page.tsx — complete landing page.
</objective>

<execution_context>
@/Users/abhishekhodavdekar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abhishekhodavdekar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-branding-and-marketing/05-01-SUMMARY.md

<interfaces>
<!-- Landing page is outside (app)/ route group — it gets NO app shell nav. Must include its own header. -->
<!-- All colors use Tailwind utility classes that map to CSS tokens from Plan 01 — bg-primary, text-primary -->
<!-- NO 'use client' — pure server component -->

Locked structure from CONTEXT.md:
- Hero: App name + tagline + "Start free" CTA
- Feature highlights: 3-4 key features (video upload, AI pose analysis, mechanics flags, coaching feedback)
- How it works: 3-step visual flow (Upload video → Get AI breakdown → Deliver feedback)
- NO: pricing, testimonials, demo video, newsletter, email capture, hero screenshot/mockup
- Feedback link on page (not email capture) — use mailto: link
- Tone: warm, community-focused, coach-athlete relationship, NOT AI-SaaS framing
- Target: baseball AND softball coaches — youth rec, high school, travel, collegiate

Copy guidance:
- Tagline direction: something like "Help every athlete reach their potential" — warm, outcome-focused
- Feature section: speak to what coaches can DO, not just what the tech does
- How it works: action-oriented step labels ("Upload a clip", "Get instant AI analysis", "Send targeted feedback")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Diamond Mechanics landing page</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the stub content in src/app/page.tsx with a full landing page. This is a server component (no 'use client').

**Structure:**
1. Standalone header (logo wordmark left, "Sign in" link right)
2. Hero section (app name, tagline, "Start free" CTA + "Sign in" secondary link)
3. Feature highlights section (4 features in a 2-col or 4-col responsive grid)
4. How it works section (3 numbered steps in a horizontal or vertical flow)
5. Bottom CTA section (repeat "Start free" + feedback link)

**Header (standalone — NOT reusing app shell):**
```tsx
<header className="border-b border-gray-100 bg-white">
  <div className="max-w-5xl mx-auto px-4 sm:px-6 py-4 flex items-center justify-between">
    <span className="font-extrabold text-xl text-gray-900">Diamond Mechanics</span>
    <a href="/login" className="text-sm font-medium text-primary hover:underline">Sign in</a>
  </div>
</header>
```

**Hero section:**
- App name as large heading: "Diamond Mechanics"
- Tagline (Claude's discretion — warm, outcome-focused, e.g., "Help every athlete reach their potential.")
- Subheading: brief description mentioning baseball and softball, AI analysis, remote coaching
- "Start free" primary CTA: `<a href="/signup">` with `bg-primary text-primary-foreground` styling
- "Sign in" secondary link below CTA

**Feature highlights (4 features):**
Choose 4 from: video upload from any device, AI pose skeleton overlay, joint angle computation, mechanics flag detection, VLM/Gemini coaching summary, remote feedback email delivery. Use Lucide React icons (import from 'lucide-react'). Grid layout: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`.
Copy tone: "As a coach, you can..." — speak to coach's capability, not the technology.

**How it works (3 steps):**
1. "Upload a clip" — drag-drop or phone camera roll, any device
2. "Get instant AI analysis" — pose skeleton, joint angles, and mechanics flags automatically
3. "Send targeted feedback" — email your athlete a clear breakdown with a link to their analysis

Layout: horizontal flex on md+ screens, vertical stack on mobile. Number each step prominently.

**Bottom CTA section:**
"Ready to coach smarter?" heading, "Start free — no credit card required" CTA, feedback link (`<a href="mailto:feedback@diamondmechanics.app?subject=Diamond%20Mechanics%20Feedback">Share feedback</a>`).

**Styling guidelines:**
- Use Tailwind classes that map to CSS tokens (bg-primary, text-primary) — do NOT hardcode colors like bg-blue-600
- Max width: max-w-5xl centered, px-4 sm:px-6 horizontal padding
- Section padding: py-16 sm:py-20 for hero, py-12 for others
- Typography: hero h1 with `text-4xl sm:text-5xl font-extrabold`, section headings with `text-2xl font-bold`
- Background: hero section with white or very light gray (`bg-gray-50`), alternating section backgrounds
- Feature cards: white background, border, rounded-xl, p-6, subtle shadow

**Anti-patterns:**
- NO 'use client'
- NO screenshots, images, videos, placeholders for images
- NO hardcoded hex colors
- NO pricing table, testimonial quotes, email input, waitlist form
  </action>
  <verify>
    <automated>cd /Users/abhishekhodavdekar/git/softball-mechanics && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - src/app/page.tsx replaces stub; no 'use client' directive
  - `grep "Start free" src/app/page.tsx` returns a match
  - `grep "href=\"/signup\"" src/app/page.tsx` returns a match
  - `grep -i "baseball" src/app/page.tsx` returns at least one match
  - `grep -i "softball" src/app/page.tsx` returns at least one match (both sports mentioned)
  - `npm run build` succeeds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Diamond Mechanics landing page at the root URL. Hero section with tagline and CTA, 4 feature highlights, 3-step how-it-works, and bottom CTA with feedback link. Standalone header with Sign in link. Brand blue applied via CSS tokens.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Verify the header shows "Diamond Mechanics" wordmark and "Sign in" link (top right)
    3. Verify the hero section has a large heading, tagline, and "Start free" button (should be Diamond blue)
    4. Verify feature highlights section has 4 features — copy mentions coaches, not just technology
    5. Verify how it works section has 3 numbered steps
    6. Verify bottom section has a "Start free" CTA and feedback/contact link
    7. Verify page copy mentions BOTH "baseball" and "softball" (not just softball)
    8. Verify NO screenshots, pricing table, testimonials, email capture form, or demo video embed
    9. Resize to mobile (375px): verify layout is readable and hero CTA is visible without scrolling
    10. Click "Start free" — should navigate to /signup (or show login/auth page if not yet signed up)
    11. Click "Sign in" in header — should navigate to /login
  </how-to-verify>
  <resume-signal>Type "approved" if the landing page looks correct, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete and checkpoint is approved:
1. `grep -c "use client" src/app/page.tsx` returns 0
2. `grep "Start free" src/app/page.tsx` returns a match
3. `grep -i "baseball" src/app/page.tsx` returns a match
4. `npm run build` succeeds
</verification>

<success_criteria>
- Landing page exists at root URL with all 3 required sections (hero, features, how it works)
- Hero has "Start free" CTA linking to /signup
- Copy mentions both baseball and softball coaches
- No prohibited elements (pricing, testimonials, demo video, email capture)
- Page is a server component (no 'use client')
- Brand blue CTA button visible in hero using CSS token (bg-primary)
- Human-verify checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-branding-and-marketing/05-02-SUMMARY.md` with:
- Landing page sections built
- Copy decisions made (tagline, feature labels)
- Checkpoint approval status
- Any deviations from plan
</output>
